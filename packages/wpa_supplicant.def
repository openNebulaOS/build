PKG_NAME="wpa_supplicant"
PKG_VERSION="2.10"
PKG_SOURCE="https://w1.fi/releases/wpa_supplicant-${PKG_VERSION}.tar.gz"
PKG_DEPS="openssl dbus"
PKG_DESCRIPTION="WPA/WPA2 client and RADIUS authenticator"

build() {
    cd wpa_supplicant
    make BINDIR=/usr/sbin DESTDIR="${PKG_DESTDIR}" install
}
