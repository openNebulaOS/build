name: Build Repository

on:
  push:
    branches: [main]
    paths:
      - 'packages/**'
      - 'repos/**'
  workflow_dispatch:

permissions:
  contents: write
  pages: write

jobs:
  build-repo:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          pacman -Sy --noconfirm base-devel git curl xz tar gzip

      - name: Build Packages
        run: |
          chmod +x ./repos/repo-update
          chmod +x ./star/star
          chmod +x ./star-build/star-build
          chmod +x ./scripts/version.sh
          mkdir -p /var/cache/star-build/{sources,packages,tmp}
          for pkg in packages/*.def; do
            ./star-build/star-build build "$pkg" -o ./repos/packages || true
          done

      - name: Update Repository Index
        run: |
          chmod +x ./repos/repo-update
          ./repos/repo-update

      - name: Setup GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./repos
          publish_branch: gh-pages
          user_name: 'openNebula CI'
          user_email: 'ci@opennebula.io'
